# Release follow-up review

| Field | Evidence |
| --- | --- |
| Type | Incremental |
| Reviewed code | `00ede93622b15fd8e71a9667a268399346c68a61` (quality PR #7) |
| Previous review | [Initial full review](2026-09-22-0934-full.md), code `55c30524f8c74198ebebd3d9d6e1340968de42a2` |
| Reviewers | Coordinating agent; independent foundation, engine and browser agents |
| Baseline commands | `scripts/check.sh`: 218 tests, formatting/lint/types, build and isolated wheel installation passed; `scripts/e2e.sh`: 11 passed in 25.91s |
| Coverage | Statements 736/750 (98.13%); branches 311/326 (95.40%); combined 97.30% |

## Summary and checked invariants

Inspected the complete 23-file code/config/test delta from the previous reviewed commit. Re-read snapshot validation and its effect/option contracts, private/public event projection, browser selection restoration, theme validation and quality scripts. The shared checks and targeted regression assertions confirm that valid interrupted games resume, rejected saves remain untouched, hidden recovery stays private, public set-aside cards reach both views and rendering, and theme changes preserve a pending local selection.

Independent reviewers rechecked the engine reproductions and browser flows. The coordinator reran both required suites on the final code and inspected the new gates and their negative cases. The installed-wheel probe imports from its disposable environment outside the checkout with isolated Python, using only locked runtime dependencies. Static searches found no unfinished implementation stubs; PostgreSQL remains an explicit deferred adapter with a migration contract.

The engine agent independently reviewed the quality PR and reran all three coverage-gate regressions, the actual coverage report and the isolated wheel installation. It confirmed failure propagation and the installed-package path guard, with no blocking finding.

## Findings

No Open or Moved findings remain. All four inherited findings are Fixed on the reviewed PR stack; the branches are not yet merged.

- `harbinger-hidden-topdeck-event` — **Fixed**, Verified in engine PR #2 (`ed83fb7`). `src/sway/engine/core.py:701` (`_answer`) restricts private topdeck events to their owner while preserving the public Bureaucrat reveal. The original interior-discard recovery reproduction was rerun independently; `test_harbinger_recovery_does_not_reveal_an_interior_discard` passes and the opponent receives no recovered-card event.
- `library-public-set-aside` — **Fixed**, Verified in engine PR #2 (`ed83fb7`) and browser PR #6 (`adc8b09`). `src/sway/engine/core.py:244` projects the zone, `core.py:724` emits its public event, and `src/sway/presentation/components.py:487` renders it. Declining the first inspected action and pausing at the next choice now exposes the set-aside action while keeping the currently inspected card private. Engine and themed-rendering regressions pass.
- `snapshot-continuation-and-id-validation` — **Fixed**, Verified in engine PR #2 (`ed83fb7`). `src/sway/engine/serialization.py:233` (`_validate_effect`), `:267` (`_validate_options`) and `:382` (`_validate`) reject the original invalid actor, reused card counter and missing pending-decision reproductions before continuation. They also check card references, decision constraints and setup consistency. All-card interrupted-decision round trips, nested attacks and replay still pass.
- `separate-engine-branch-coverage-gate` — **Fixed**, Verified in quality PR #7 (`7f55b7b`). `scripts/coverage.sh:9` invokes `scripts/check-coverage.py`; its integer threshold at line 58 rejects 89/100 branches despite 100% statement coverage, accepts 90/100 and fails absent branch data. Those subprocess regressions pass. The real engine reports 311/326 branches, above the required 90%.

## Queue mapping and work order

| Backlog item | Findings | Outcome |
| --- | --- | --- |
| `base-rules-review-corrections` | `harbinger-hidden-topdeck-event`, `library-public-set-aside`, `snapshot-continuation-and-id-validation` | Verified fixed; removed from promoted queue |
| `enforce-engine-branch-coverage` | `separate-engine-branch-coverage-gate` | Verified fixed; removed from promoted queue |

No unmapped inventory or new review work is retained. Merge the reviewed PR stack in dependency order, rebasing later branches after prerequisites land. The user owns merge and release authorization. [Release evidence](../ACCEPTANCE.md) maps the accepted criteria to the checks.

## Verification limits

Local checks used macOS, Python 3.12.14 and Chromium; browser execution required permission outside the macOS process sandbox. The Linux required CI check on PR #7 also passed, including browser and installed-wheel checks. Other browser engines, remote hosting, human multiplayer and PostgreSQL are outside this release. Bot profiles are heuristics. Two upstream test-client deprecation warnings remain visible. Original names and art are an implementation choice, not legal clearance. No deployment, merge or release tag was performed.
